<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://code.alibabatech.com/schema/dubbo
        http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

	<!-- 提供方应用信息，用于计算依赖关系 -->
	<dubbo:application name="${dubbo.application.name}" />
	<!-- 使用zookeeper注册中心暴露服务地址 -->
	<dubbo:registry protocol="zookeeper" address="${dubbo.registry.address}" />
	<!-- 用dubbo协议在20880端口暴露服务 -->
	<dubbo:protocol name="${dubbo.protocol.name}" port="${dubbo.protocol.port}" />
	<!-- 当ProtocolConfig和ServiceConfig某属性没有配置时,采用此缺省值 -->
	<dubbo:provider timeout="15000" threadpool="fixed" threads="100" accepts="1000" />
	
	<!-- <dubbo:monitor protocol="registry" /> -->

	<!-- 数据库操作接口 -->
	<dubbo:service retries="0" interface="com.aaden.pay.api.DbBankService" ref="dbBankService" version="${dubbo.own.version}" />
	<dubbo:service retries="0" interface="com.aaden.pay.api.DbPayService" ref="dbPayService" version="${dubbo.own.version}" />
	<!-- 支付业务接口 -->
	<dubbo:service retries="0" interface="com.aaden.pay.api.BankService" ref="bankService" version="${dubbo.own.version}" />
	<dubbo:service retries="0" interface="com.aaden.pay.api.PaymentService" ref="paymentService" version="${dubbo.own.version}" />
	<dubbo:service retries="0" interface="com.aaden.pay.api.PayTaskService" ref="payTaskService" version="${dubbo.own.version}" />
</beans>
